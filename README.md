# Data-Cleaning-in-SQL
This project demonstrates a complete SQL-based data cleaning pipeline applied to a global layoffs dataset.   The workflow preserves raw data, removes duplicates, standardizes values, and imputes missing fields to prepare the dataset for reliable analysis.
